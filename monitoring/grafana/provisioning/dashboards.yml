apiVersion: 1

providers:
  - name: ServantAssist
    folder: ServantAssist
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
